// Copy this file to config.js and fill in your values.
// config.js is gitignored — never commit it directly.
window.JULIE_CONFIG = {
  GLM_BASE_URL: "https://your-glm-endpoint/v1",
  GLM_MODEL: "glm-5.1-fp8",
  GLM_API_KEY: "",          // leave empty if no auth required
};
